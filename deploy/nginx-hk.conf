# 港服 Nginx 配置
# 作为入口服务器，反向代理到乌服

upstream lzti_backend {
    # 乌服的内网 IP 和端口
    server 172.21.92.160:3000;
}

server {
    listen 80;
    server_name lzti.nzyy.cc;

    # 强制跳转到 HTTPS（生产环境建议开启）
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://lzti_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态资源缓存
    location /_next/static {
        proxy_pass http://lzti_backend;
        proxy_cache_valid 200 60m;
        expires 60m;
        add_header Cache-Control "public, immutable";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # API 代理
    location /api {
        proxy_pass http://lzti_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查
    location /health {
        proxy_pass http://lzti_backend;
        access_log off;
    }

    # 日志
    access_log /var/log/nginx/lzti_access.log;
    error_log /var/log/nginx/lzti_error.log;
}

# HTTPS 配置（需要 SSL 证书）
# 使用 certbot 自动生成证书后取消注释
# server {
#     listen 443 ssl http2;
#     server_name lzti.nzyy.cc;
#
#     ssl_certificate /etc/letsencrypt/live/lzti.nzyy.cc/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/lzti.nzyy.cc/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://lzti_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location /api {
#         proxy_pass http://lzti_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
